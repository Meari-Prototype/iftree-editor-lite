# IFTreeEditor 导入导出测试夹具

本文档是 IFTreeEditor 导入/导出测试专用夹具，用于反复「导入 → 校验结构 → 导出 → 删除重导」。它不承载任何真实业务，可以随时删除重建，请勿把其中内容当成真实需求。稳定标识：IOFX-DOC-20260620。

## 第一章 入口条件

IOFX_ALPHA 第一章正文，用于校验二级标题被导入成带子节点的章节，并在导出时还原成二级标题。

### 1.1 深层子节点

IOFX_BETA 三级标题正文，用于校验更深一层的层级映射与地址递增。

## 第二章 主流程

IOFX_GAMMA 第二章正文，用于校验多个并列章节的导入顺序与导出顺序保持一致。

### 2.1 步骤说明

IOFX_DELTA 步骤正文。本节末尾的 IOFX_END 标记用于校验导出未截断尾部内容。
